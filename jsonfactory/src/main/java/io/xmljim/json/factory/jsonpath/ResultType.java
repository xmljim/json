package io.xmljim.json.factory.jsonpath;

public enum ResultType {
    /**
     * Return all values from a JsonPath query
     */
    VALUE,
    /**
     * Return the paths for each result
     */
    PATH
}
